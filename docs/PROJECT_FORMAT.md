# プロジェクト保存形式

`Save As` では、現在の状態をプロジェクトファイルとして保存します。

保存対象の例:

- メンバー
- 属性スキーマ
- 属性型
- Balance値
- Pair rules
- グループ割り当て
- スロット位置
- 固定状態
- グループ数
- グループ名
- Display設定
- General設定

## 互換性

内部仕様は更新されることがありますが、可能な範囲で旧形式も読み込めるようにしています。

GitHub上で公開する場合、プロジェクト保存形式は将来的に別途正式仕様として固定する可能性があります。
